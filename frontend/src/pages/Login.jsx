import {useState} from 'react';import {api} from '../services/api';export default function Login({onLogin,onSwitch}){const[f,setF]=useState({email:'',password:''}),[error,setError]=useState('');async function submit(e){e.preventDefault();try{const d=await api('/users/login',{method:'POST',body:JSON.stringify(f)});localStorage.setItem('taskflow_token',d.token);onLogin(d.user)}catch(e){setError(e.message)}}return <div className="auth"><div className="authCard"><h1>Welcome back.</h1><p>Sign in to continue to TaskFlow.</p><form onSubmit={submit}><input type="email" placeholder="Email" value={f.email} onChange={e=>setF({...f,email:e.target.value})} required/><input type="password" placeholder="Password" value={f.password} onChange={e=>setF({...f,password:e.target.value})} required/>{error&&<div className="err">{error}</div>}<button className="primary">Sign in</button></form><button className="switch" onClick={onSwitch}>Create an account</button></div></div>}
