import {useState} from 'react';import {api} from '../services/api';export default function Register({onRegister,onSwitch}){const[f,setF]=useState({name:'',email:'',password:''}),[error,setError]=useState('');async function submit(e){e.preventDefault();try{const d=await api('/users/register',{method:'POST',body:JSON.stringify(f)});localStorage.setItem('taskflow_token',d.token);onRegister(d.user)}catch(e){setError(e.message)}}return <div className="auth"><div className="authCard"><h1>Create your workspace.</h1><p>Start organizing your day with TaskFlow.</p><form onSubmit={submit}><input placeholder="Full name" value={f.name} onChange={e=>setF({...f,name:e.target.value})} required/><input type="email" placeholder="Email" value={f.email} onChange={e=>setF({...f,email:e.target.value})} required/><input type="password" placeholder="Password (6+ characters)" minLength="6" value={f.password} onChange={e=>setF({...f,password:e.target.value})} required/>{error&&<div className="err">{error}</div>}<button className="primary">Create account</button></form><button className="switch" onClick={onSwitch}>Already have an account?</button></div></div>}
